#[cfg(test)]
mod tests_parser {
    use star_line::functions;
    use star_line::functions::parser;

    #[test]
    fn convert_simple_line() {
        let line: &str = "1001";
        assert_eq!(parser::into_vec(line).unwrap(), Vec{1usize, 0usize, 0usize, 1usize});
    }

}